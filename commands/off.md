---
description: less → off (normal Claude verbosity)
---

Set the less protocol to off.

Run: `mkdir -p ~/.claude && printf off > ~/.claude/less.mode`

Then reply in ONE line: `less → off (from next turn)`. Nothing else.
