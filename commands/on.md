---
description: less → on (default — minimal, answer-first, pick-lists)
---

Set the less protocol to on.

Run: `mkdir -p ~/.claude && printf on > ~/.claude/less.mode`

Then reply in ONE line: `less → on (from next turn)`. Nothing else.
