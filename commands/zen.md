---
description: less → zen (near-silent — results and pick-lists only)
---

Set the less protocol to zen.

Run: `mkdir -p ~/.claude && printf zen > ~/.claude/less.mode`

Then reply in ONE line: `less → zen (from next turn)`. Nothing else.
